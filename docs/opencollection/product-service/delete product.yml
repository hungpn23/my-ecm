info:
  name: delete product
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{product}}/products/{{product_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
