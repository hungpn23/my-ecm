info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{identity}}/logout"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
