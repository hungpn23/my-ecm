info:
  name: google callback
  type: http
  seq: 7

http:
  method: GET
  url: "{{identity}}/google/callback"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
