info:
  name: sign-in
  type: http
  seq: 2

http:
  method: POST
  url: "{{identity}}/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "hungpn23@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
