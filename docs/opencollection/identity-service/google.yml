info:
  name: google
  type: http
  seq: 6

http:
  method: GET
  url: "{{identity}}/google"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
