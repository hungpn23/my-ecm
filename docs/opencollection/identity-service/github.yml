info:
  name: github
  type: http
  seq: 8

http:
  method: GET
  url: "{{identity}}/github"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
