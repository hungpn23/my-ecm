info:
  name: profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{identity}}/auth/profile"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
