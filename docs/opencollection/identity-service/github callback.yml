info:
  name: github callback
  type: http
  seq: 9

http:
  method: GET
  url: "{{identity}}/github/callback"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
