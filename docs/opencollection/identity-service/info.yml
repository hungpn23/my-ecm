info:
  name: info
  type: http
  seq: 5

http:
  method: GET
  url: "{{identity}}/users/info"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
